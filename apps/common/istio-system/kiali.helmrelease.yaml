apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kiali
  namespace: istio-system
spec:
  interval: 1m
  chart:
    spec:
      chart: kiali-operator
      version: 1.44.0
      sourceRef:
        kind: HelmRepository
        name: kiali
        namespace: istio-system
      interval: 1m